server:
  port: 9999
#  configure for the sentinel-dashboard
spring:
  cloud:
    sentinel:
      transport:
        port: 8719
        dashboard: 192.168.10.229:8080
feign:
  sentinel:
    enabled: true
#  circuitbreaker:
#    enabled=true: